{
  "name": "barberbook",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm install && npx prisma generate && cd Barberbook && npm install && npm run build"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0",
    "prisma": "^5.22.0"
  }
}